{
  "name": "skyfall-sky-demo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Interactive demo app for testing Skyfall's monitor-to-fix loop end-to-end. Ships with intentional bugs that fire to Sentry; Skyfall picks them up, drafts Linear tickets, and ships fixes via PR.",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@sentry/react": "^8.45.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
